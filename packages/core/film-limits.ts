// A single film remains bounded even when each source clip is much shorter.
export const MAX_FILM_SECONDS = 60;
export const MAX_FILM_SHOTS = 8;
export const MAX_VOICE_TRACKS = 12;
export const MAX_AUDIO_TRACKS = MAX_VOICE_TRACKS + 2; // music and effect
